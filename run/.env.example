# Spring Servlet web-server
SERVER_PORT=8080

# stateless JWT authentication
JWT_SECRET=
JWT_ISSUER=Minigram
JWT_AUDIENCE=MinigramClient
JWT_EXPIRATION=15

# database (postgres profile)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=minigram_chats
DB_USERNAME=minigram
DB_PASSWORD=minigram
