ktor.application.modules = []

chats {
  database {
    url = "jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_UPPER=FALSE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE"
    driver = "org.h2.Driver"
    username = ""
    password = ""
    maxPoolSize = 5
    minIdle = 1
    migrationLocations = ["classpath:db/test-migration"]
  }
  jwt {
    secret = "test-secret-at-least-32-characters-long"
    issuer = "TestIssuer"
    audience = "TestAudience"
  }
  services {
    profileUrl = "http://localhost:9999"
  }
}
